# Hermes Compatibility

Status: designed; E2E `not_run`.

Entry: root `AGENTS.md`. User domain Skill Wrapper installs to the active profile's `skills/` directory. Reload through `/reload-skills` or start a new session.

Static compatibility does not prove runtime behavior or MCP authentication.
