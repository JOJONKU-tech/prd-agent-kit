# Codex Compatibility

Status: designed; E2E `not_run`.

Entry: root `AGENTS.md`. User domain Skills default to `~/.agents/skills/`; project scope uses `.agents/skills/`. The old `$CODEX_HOME/skills` path is legacy only.

This status is based on current official conventions and static checks, not a completed runtime E2E.
