# Claude Code Compatibility

Status: designed; E2E `not_run`.

Entry chain: `CLAUDE.md` → `@AGENTS.md` → Canonical repository Skill.

User domain Skills use a thin Wrapper in `~/.claude/skills/` by default. The project path is `.claude/skills/`. Platform-only Frontmatter stays in the Wrapper.

This status is not a claim of full runtime verification. Follow `../testing/e2e/claude-code.md` to record a real run.
