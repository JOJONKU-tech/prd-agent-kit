# Hermes MCP Setup

1. Check existing servers and authentication prerequisites.
2. Use `hermes mcp add` only after user confirmation.
3. Use `hermes mcp test` and inspect discovered Tool Schemas.
4. Generate a Generic Document Adapter proposal.
5. Keep credentials in Hermes/MCP configuration, never in repository files.
6. Request separate sandbox write authorization before formal publishing.

Official documentation:
https://hermes-agent.nousresearch.com/docs/user-guide/features/mcp
